plugins {         id "org.sonarqube" version "2.6.2" apply false   }
subprojects { subproject ->
  apply plugin: 'java'
  apply plugin: 'org.sonarqube'

  repositories {
    jcenter()
  }
dependencies {
compile 'com.google.guava:guava-testlib:27.0.1-jre'
}
}
